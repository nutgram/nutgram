name: release.yml

on:
  release:
    types: [ released ]

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Repository Dispatch
        uses: peter-evans/repository-dispatch@v4
        with:
          token: ${{ secrets.PAT }}
          repository: nutgram/laravel
          event-type: nutgram-release
